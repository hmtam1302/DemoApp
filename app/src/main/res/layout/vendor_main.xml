<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="690dp"
        android:id="@+id/vendorContainer"/>
    <!--Navigation bar-->
    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <TableRow>
            <ImageButton
                android:id="@+id/vendor_stat_btn"
                android:layout_width="140dp"
                android:layout_height="40dp"
                android:adjustViewBounds="true"
                android:background="#FFFFFF"
                android:scaleType="fitCenter"
                android:src="@drawable/statistic_pressed"
                android:onClick="displayStat"/>

            <ImageButton
                android:id="@+id/vendor_setting_btn"
                android:layout_width="140dp"
                android:layout_height="40dp"
                android:adjustViewBounds="true"
                android:background="#FFFFFF"
                android:onClick="displayVendorSettings"
                android:scaleType="fitCenter"
                android:src="@drawable/settings" />
            <ImageButton
                android:id="@+id/vendor_info_btn"
                android:layout_width="140dp"
                android:layout_height="40dp"
                android:adjustViewBounds="true"
                android:background="#FFFFFF"
                android:onClick="displayInfo"
                android:scaleType="fitCenter"
                android:src="@drawable/account_btn" />
        </TableRow>
    </TableLayout>
</RelativeLayout>